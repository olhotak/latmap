package latmap

class Solver {
  def solve(program: Program) = {
    // TODO: create a plan for each pair (Rule, Atom position in the rule)
    // TODO: then write an algorithm to iterate through the plans until a fixed point is reached
  }
}
